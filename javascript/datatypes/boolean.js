let male = false;

let r = Math.round(Math.random());

male = r ? true: false;

console.log(male);

if (male) {
    console.log("We will use name John");
} else {
    console.log("We will use name Victoria");
}
